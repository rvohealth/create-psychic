{
  "name": "@rvohealth/psychic-cli",
  "version": "0.2.1",
  "description": "psychic cli",
  "main": "src/dist/psy.js",
  "repository": "https://github.com/rvohealth/psychic-cli.git",
  "author": "avocadojesus",
  "license": "Apache-2.0",
  "bin": {
    "psy": "./dist/src/psy.js"
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "publish": "yarn npm publish",
    "build": "mkdir -p dist && rm -r dist && tsc -p tsconfig.json && rm -r dist/boilerplate && cp -r boilerplate dist/boilerplate/",
    "psy": "npx ts-node ./src/psy.ts",
    "prepack": "yarn build"
  },
  "dependencies": {
    "@types/node": "^22.6.1",
    "colorette": "^2.0.20",
    "commander": "^10.0.1",
    "ora": "^6.3.0",
    "pino-std-serializers": "^4.0.0",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "prettier": "^2.8.7",
    "ts-node": "^10.9.1"
  },
  "packageManager": "yarn@4.5.1"
}
