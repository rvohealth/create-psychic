import { PsychicRouter } from '@rvoh/psychic'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function internalRoutes(r: PsychicRouter) {
  // add routes here, perhaps by running `{{PM}} psy g:resource internal/pets Pet name:citext birthdate:date species:enum:pet_species:dog,cat,fish`
}
