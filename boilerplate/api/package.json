{
  "name": "psychic-app",
  "version": "0.0.1-beta1",
  "main": "src/index.ts",
  "license": "Apache 2.09",
  "private": true,
  "scripts": {
    "psy": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/psychic psy",
    "psyjs": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/psychic psyjs",
    "build": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/psychic build && echo \"building your app...\" && rm -rf dist && APP_ROOT_PATH=$(pwd) npx tsc -p ./tsconfig.build.json && yarn after-build",
    "after-build": "cp ./src/conf/app.yml ./dist/src/conf/app.yml && cp -R ./openapi ./dist",
    "dream": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/dream dream",
    "dreamjs": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/dream dreamjs",
    "dev": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) NODE_ENV=development TS_SAFE=1 WORKER_COUNT=0 ts-node --transpile-only ./src/main.ts",
    "dev-worker": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) NODE_ENV=development TS_SAFE=1 WORKER_COUNT=1 ts-node --transpile-only ./src/worker.ts",
    "prod": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) NODE_ENV=production node ./dist/src/main.js",
    "worker": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) NODE_ENV=production node ./dist/src/worker.js",
    "sync": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version yarn psy sync:all",
    "uspec": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) TS_SAFE=1 jest -w 1 --config=./spec/unit/jest.config.js --forceExit --verbose --logHeapUsage",
    "fspec": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version APP_ROOT_PATH=$(pwd) TS_SAFE=1 jest --clearCache && jest --config=./spec/features/jest.config.js --runInBand --forceExit --verbose --logHeapUsage",
    "console": "APP_NAME=$npm_package_name APP_VERSION=$npm_package_version TS_SAFE=1 APP_ROOT_PATH=$(pwd) npx ts-node --transpile-only --project ./tsconfig.json ./src/conf/repl.ts"
  },
  "dependencies": {
    "@rvohealth/dream": "^0.1.92",
    "@rvohealth/psychic": "^0.1.66"
  },
  "devDependencies": {
    "@types/expect-puppeteer": "^5.0.3",
    "@types/jest-environment-puppeteer": "^5.0.3",
    "@types/supertest": "^2.0.12",
    "express-requests-logger": "^4.0.0",
    "jest-puppeteer": "^8.0.6",
    "puppeteer": "^19.9.1",
    "supertest": "^6.3.3",
    "typescript": "^5.3.3"
  }
}
