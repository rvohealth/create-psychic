{
  "name": "api",
  "version": "0.0.0",
  "description": "api",
  "main": "src/app.ts",
  "license": "Apache-2.0",
  "scripts": {
    "psy": "yarn --cwd=api psy",
    "dream": "yarn --cwd=api dream",
    "sync": "yarn --cwd=api sync",
    "dev": "yarn --cwd=api dev",
    "uspec": "yarn --cwd=api uspec",
    "fspec": "yarn --cwd=api fspec",
    "console": "yarn --cwd=api console",
    "c": "yarn --cwd=api c"
  }
}
